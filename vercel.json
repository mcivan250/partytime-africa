{
  "installCommand": "cd mobile && npm install",
  "buildCommand": "cd mobile && npx expo export --platform web && node scripts/inject-og.js",
  "outputDirectory": "mobile/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
